<form>
  <h2>{{localize 'RQG.Settings.Hitlocations.settingName'}}</h2>
  <input type="text" name="hitLocationItemNames" placeholder="{{localize 'RQG.Settings.Hitlocations.addLocationPlaceholder'}}"><br><br>
  <div class="flexrow">
    {{#each hitLocationItemNames}}
        <input type="text" value="{{this}}" name="hitLocationItemNames"><br>
    {{/each}}
  </div>
</form>
