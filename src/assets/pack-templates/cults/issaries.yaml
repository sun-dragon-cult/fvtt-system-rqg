# issaries
name: ${{openSystem.content.cult.issaries.name}}$
type: cult
img: systems/rqg/assets/images/items/cult.svg
system:
  deity: ${{openSystem.content.cult.issaries.name}}$
  joinedCults:
    - cultName: ${{openSystem.content.cult.issaries.name}}$
      tagline: ${{openSystem.content.cult.issaries.tagline}}$
      rank: layMember
  runePoints:
    value: 0
    max: 0
  holyDays:
  runes:
    - ${{openSystem.content.runeDescription.harmonyPower.name}}$
    - ${{openSystem.content.runeDescription.movementPower.name}}$
  descriptionRqidLink:
    rqid: je..issaries
    name: ${{openSystem.content.cult.issaries.name}}$
flags:
  rqg:
    documentRqidFlags:
      id: i.cult.issaries
      lang: ${{openSystem.lang}}$
      priority: ${{openSystem.prio}}$
