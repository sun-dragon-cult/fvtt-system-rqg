# magasta
name: ${{openSystem.content.cult.magasta.name}}$
type: cult
img: systems/rqg/assets/images/items/cult.svg
system:
  deity: ${{openSystem.content.cult.magasta.name}}$
  joinedCults:
    - cultName: ${{openSystem.content.cult.magasta.name}}$
      tagline: ${{openSystem.content.cult.magasta.tagline}}$
      rank: layMember
  runePoints:
    value: 0
    max: 0
  holyDays:
  runes:
    - ${{openSystem.content.runeDescription.movementPower.name}}$
    - ${{openSystem.content.runeDescription.waterElement.name}}$
    - ${{openSystem.content.runeDescription.waterElement.name}}$
    - ${{openSystem.content.runeDescription.deathPower.name}}$
  descriptionRqidLink:
    rqid: je..magasta
    name: ${{openSystem.content.cult.magasta.name}}$
flags:
  rqg:
    documentRqidFlags:
      id: i.cult.magasta
      lang: ${{openSystem.lang}}$
      priority: ${{openSystem.prio}}$
